<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swimtrainer" >

    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SWIMTRAINER" >
        <activity android:name=".rejestracja" >
        </activity>
        <activity android:name=".plan5" />
        <activity android:name=".plan4" />
        <activity android:name=".plan3" />
        <activity android:name=".plan2" />
        <activity android:name=".plan11" />
        <activity android:name=".rekord" />
        <activity android:name=".Logowanie" />
        <activity android:name=".Cwiczenia" />
        <activity android:name=".PlanyTreningowe" />
        <activity android:name=".Glowne" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>